<div class="vote-form votebar" id='vote-form'>

  <form class='button-form' method="post" action='/<%= voteable.class.name.downcase %>s/<%= voteable.id %>/upvote' class="inline">
    <button class="upvote" type="submit" name="upvote" class="vote-button" id="upvote-button"></button>
  </form>

  <p>
    <span class='votes'><%= voteable.get_score %></span>
  </p>
  <form class='button-form' method="post" action='/<%= voteable.class.name.downcase %>s/<%= voteable.id %>/downvote' class="inline">
    <button class="downvote" type="submit" name="downvote" class="vote-button" id="downvote-button"></button>
  </form>
</div>
